<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exodus_QFirebase"
        tools:targetApi="31">

        <activity android:name="Persona"
            android:label="Ejemplo SQLite"
            android:exported="true"/>

        <activity
            android:name=".Main"
            android:exported="true"/>

        <activity
            android:name=".Sensores"
            android:screenOrientation="portrait"/>

        <activity android:name=".Splash"
                  android:exported="true"/>

            <intent-filter>
                <action   android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        <activity android:name=".Main_Text"
                  android:exported="true"/>

        <activity android:name=".Map"/>

        <activity android:name=".Persona_Read"
                  android:label="Leer SQLite"/>

        <activity android:name=".Persona_Edit"
                  android:label="Actualizar SQLite"/>

        <activity android:name=".Firebase"/>

        <activity android:name=".Firebase_List"/>

        <activity android:name=".Firebase_Update"/>
    </application>
</manifest>